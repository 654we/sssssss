# DisplayAffinityManager
A tool to easily set window's display affinity.

## Auto-detect mode
Create or edit `DisplayAffinityManager.ini` in the same directory as the executable and list the target process names to automatically set them to normal display affinity (WDA_NONE) when they launch.
