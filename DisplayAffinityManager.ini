[AutoDetect]
; Enable auto-detect to remove capture protection when targets launch.
Enabled=1
; Target process names (case-insensitive), separated by semicolons or commas.
Targets=example.exe;game.exe
; Polling interval in milliseconds (minimum 200).
PollIntervalMs=2000
